<- Vertex ----------------------------------------------------------------------

The Vertex class handles vertexes.

An instance can be contructed with three coordinates x, y and z, and an optional color:
new Vertex( array( x, y, z [color = new color]) );

---------------------------------------------------------------------- Vertex ->
